services:
  fuseki:
    build:
      context: .
      dockerfile: Dockerfile
    image: fuseki
    ports:
      - "3030:3030"
    volumes:
      - ./run:/fuseki/run
      - ./tmp:/fuseki/tmp
      - ./jena:/opt/jena 
    environment:
      FUSEKI_ADMIN_USER: admin
      FUSEKI_ADMIN_PASSWORD: admin